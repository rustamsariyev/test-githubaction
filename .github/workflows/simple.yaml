name: Shell Command

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Second string"
      - name: multiline sript
        run: |
          node -v
          npm -v